# appdev-starter

โครงสร้างโปรเจกต์ตัวอย่างสำหรับระบบคอร์สเรียนที่มี 2 ส่วนหลัก:

1. **LIFF Frontend** (`src/liff-web`) สำหรับลูกค้า ใช้งานผ่าน LINE OA Rich Menu  
2. **Admin Back-office** (`src/admin-web`) สำหรับผู้ดูแลระบบ  
3. **Backend API** (`src/api`) สำหรับเชื่อมต่อฐานข้อมูลและ Omise  
4. **PostgreSQL Database** รันด้วย Docker

ทั้งหมดถูกรันผ่าน **Docker Compose** เพื่อให้คุณทดลองบนเครื่องตัวเอง และใช้ ngrok เปิด https สำหรับ LIFF ได้ง่าย ๆ

---

## Role & Feature Mapping (Back-office)

| Role | Access Scope | ตัวอย่างฟีเจอร์ |
| --- | --- | --- |
| **Super Admin** | Global (ทุกสาขา) | • เข้าถึงทุกหน้าในหลังบ้าน<br>• สร้าง/แก้ไขคอร์สทุกสาขา<br>• จัดการผู้ใช้หลังบ้านและสิทธิ์ (Users)<br>• ดูยอดขาย/คำสั่งซื้อ/การชำระเงินทั้งหมด |
| **Branch Admin** | จำกัดภายในสาขาที่รับผิดชอบ | • ดูแดชบอร์ดและจัดการคอร์สของสาขาตนเอง<br>• สร้างหรือแก้ไขคอร์ส (เฉพาะสาขา)<br>• เข้าถึงข้อมูลลูกค้า/คำสั่งซื้อเฉพาะสาขา |
| **Instructor** | เฉพาะคอร์สที่สอน | • ดูแดชบอร์ดข้อมูลคลาสของตัวเอง<br>• ตรวจสอบรายชื่อนักเรียน/สิทธิ์เข้าเรียน<br>• ไม่มีสิทธิ์ปรับข้อมูลเชิงโครงสร้างหรือผู้ใช้ |

> ทุก role จะได้รับ JWT ที่มีฟิลด์ `role` และ `permissions` เพื่อให้ทั้ง Backend/Frontend ใช้ประกอบการตัดสินใจเปิดฟีเจอร์ตามสิทธิ์

---

## 1. สิ่งที่ต้องมีก่อนเริ่ม

1. ติดตั้ง **Docker Desktop** หรือ Docker Engine + docker-compose  
2. ติดตั้ง **Node.js 18+** (ถ้าคุณอยากรันนอก Docker)  
3. สมัครและติดตั้งเครื่องมือ tunnel อย่างใดอย่างหนึ่ง เช่น:
   - [ngrok](https://ngrok.com/) (แนะนำเพื่อง่าย)  

4. มี LINE Developers Account เพื่อสร้าง LIFF

---

## 2. โครงสร้างโปรเจกต์

```text
project-root/
  docker-compose.yml
  README.md
  docker/
    api/Dockerfile
    liff-web/Dockerfile
    admin-web/Dockerfile
    db/init.sql
  src/
    api/
      package.json
      .env.example
      src/
        server.js
        db.js
    liff-web/
      package.json
      .env.example
      index.html
      vite.config.mjs
      src/
        main.jsx
        App.jsx
        pages/
          Home.jsx
          Courses.jsx
          HowTo.jsx
          Contact.jsx
    admin-web/
      package.json
      .env.example
      index.html
      vite.config.mjs
      src/
        main.jsx
        App.jsx
        pages/
          Dashboard.jsx
          Courses.jsx
          Users.jsx
```

---

## 3. การตั้งค่าเบื้องต้น

### 3.1 Backend API (`src/api`)

1. คัดลอกไฟล์ `.env.example` เป็น `.env` (สำหรับรันนอก Docker ถ้าต้องการ)  

   ```bash
   cd src/api
   cp .env.example .env
   ```

2. ค่าใน `.env.example` ถูกตั้งมาให้ใช้กับ Docker Compose แล้ว:

  ```env
  PORT=4000
  DATABASE_URL=postgres://appuser:applocalpass@db:5432/appdb
  OMISE_PUBLIC_KEY=omise_public_key_here
  OMISE_SECRET_KEY=omise_secret_key_here
  CORS_ORIGIN_LIFF=http://localhost:3000
  CORS_ORIGIN_ADMIN=http://localhost:3001
  JWT_ACCESS_SECRET=dev-access-secret
  JWT_REFRESH_SECRET=dev-refresh-secret
  ACCESS_TOKEN_TTL=15m
  REFRESH_TOKEN_TTL=7d
  ```

  ภายหลัง คุณสามารถอัปเดต OMISE keys เป็นค่า test จริงจาก Omise ได้ รวมถึงตั้งค่า secret/token ttl ให้เหมาะกับ production. ค่าเริ่มต้นมีการ seed ผู้ใช้หลังบ้านเอาไว้คือ `admin@yoga.local / Admin123!` (role = `super_admin`) เพื่อให้ทดสอบ flow ได้ทันที

  #### Seed / อัปเดตบัญชีแอดมิน

  หากคุณต้องการสร้างหรืออัปเดตบัญชีแอดมินด้วยตัวเอง สามารถใช้สคริปต์ seed ได้ดังนี้

  ```bash
  cd src/api
  npm run seed:admin -- --email=admin@yoga.local --password=Admin123! --name="Default Admin" --role=super_admin
  ```

  * ไม่ใส่พารามิเตอร์จะ fallback เป็นค่าด้านบน
  * สามารถกำหนด `--permissions=courses:read,courses:write` หรือใช้ env `SEED_ADMIN_*` ได้
  * รันซ้ำเท่าไรก็ได้ สคริปต์จะ upsert ให้ (อัปเดตรหัสผ่าน/สิทธิ์ให้ตรงกับค่าที่ส่งเข้าไป)

---

### 3.2 LIFF Frontend (`src/liff-web`)

1. คัดลอก `.env.example` เป็น `.env` (ถ้าจะรันนอก Docker):

   ```bash
   cd src/liff-web
   cp .env.example .env
   ```

2. ค่าเริ่มต้น:

   ```env
   VITE_API_BASE_URL=http://localhost:4000
   VITE_LIFF_ID=YOUR_LIFF_ID_HERE
   ```

   - `VITE_API_BASE_URL` จะชี้ไปยัง API
   - `VITE_LIFF_ID` ไว้ใช้ตอนเชื่อม LINE LIFF จริง 

> ในโค้ดปัจจุบันเน้นเทส API + UI ยังไม่ได้ผูก LIFF SDK จริง คุณสามารถเพิ่มภายหลังได้

---

### 3.3 Admin Frontend (`src/admin-web`)

1. คัดลอก `.env.example` เป็น `.env` (ถ้าจะรันนอก Docker):

   ```bash
   cd src/admin-web
   cp .env.example .env
   ```

2. ค่าเริ่มต้น:

   ```env
   VITE_API_BASE_URL=http://localhost:4000
   ```

---

## 4. การรันด้วย Docker Compose (แนะนำ)

จาก root ของโปรเจกต์ (โฟลเดอร์เดียวกับ `docker-compose.yml`):

```bash
docker-compose build
docker-compose up
```

Docker Compose จะรัน service ดังนี้:

- **db** (PostgreSQL): `localhost:5432`
- **api** (Express): `localhost:4000`
- **liff-web** (React Vite): `localhost:3000`
- **admin-web** (React Vite): `localhost:3001`

### 4.0.1 Admin Dashboard API Response Shape

ฝั่งหลังบ้านสามารถดึงข้อมูลสรุปแดชบอร์ดผ่าน `GET /api/admin/dashboard` (แนบ `Authorization: Bearer <admin_access_token>`). เซิร์ฟเวอร์จะรวมผลลัพธ์จาก SQL aggregation หลายคำสั่งแล้วคืนค่าโครงสร้างดังนี้:

```json
{
  "kpis": {
    "totalSalesCents": 1250000,
    "totalOrders": 42,
    "paidOrdersToday": 3,
    "newMembers30d": 18,
    "averageOrderValueCents": 29750
  },
  "charts": {
    "ordersByDay": [
      { "date": "2024-06-10", "revenueCents": 120000, "ordersCount": 2 },
      { "date": "2024-06-11", "revenueCents": 450000, "ordersCount": 5 }
    ],
    "newMembersByDay": [
      { "date": "2024-06-10", "newMembers": 1 },
      { "date": "2024-06-11", "newMembers": 3 }
    ],
    "topCourses": [
      { "courseId": 7, "title": "Yin Yoga", "ordersCount": 8, "revenueCents": 520000 }
    ]
  },
  "generatedAt": "2024-06-11T08:30:00.000Z"
}
```

ข้อมูล `kpis` คือค่ารวม เช่น ยอดขายรวม (อิง `payments` ที่ `status = 'successful'`) หรือจำนวนสมาชิกใหม่ 30 วันย้อนหลัง ส่วน `charts` ใช้ป้อนให้กราฟ/ตารางเชิงเวลา เช่น ยอดสั่งซื้อรายวัน, สมาชิกใหม่รายวัน และคอร์สยอดนิยม 5 อันดับแรก โดย API จะ refresh ข้อมูลทุกครั้งที่เรียก จึงสามารถใช้แสดง Dashboard แบบ real-time ได้ทันที

### 4.1 ทดสอบเบื้องต้น

1. เปิดเบราว์เซอร์ไปที่:
   - LIFF mock: `http://localhost:3000`
   - Admin: `http://localhost:3001`
2. ทดสอบ API health check:

   ```bash
   curl http://localhost:4000/health
   ```

   ถ้าเชื่อม DB สำเร็จจะได้ JSON ประมาณ:

   ```json
   { "status": "ok", "db": "connected" }
   ```

3. ทดสอบสร้างคอร์สจากหน้า Admin:
   - เข้า `http://localhost:3001/courses`
   - เติมฟอร์ม “สร้างคอร์ส”
   - กดสร้าง → จะยิง `/courses` ไปที่ API และบันทึกลง Postgres
   - หน้า LIFF (`/courses`) ก็จะสามารถดึงรายการคอร์สมาแสดงได้

---

## 5. การเทส LIFF จริงผ่าน https ด้วย ngrok

เนื่องจาก LIFF ต้องการให้ endpoint เป็น `https://` และต้องเข้าถึงได้จากอินเทอร์เน็ต คุณสามารถใช้ ngrok เพื่อเปิดทางจากโลกภายนอกมาที่เครื่องคุณ

### 5.1 ติดตั้งและเข้าสู่ระบบ ngrok

1. สมัครบัญชีที่ [https://ngrok.com/](https://ngrok.com/)  
2. ดาวน์โหลดและติดตั้ง `ngrok` ตาม OS  
3. ทำการเชื่อม token ตามคู่มือของ ngrok

### 5.2 เปิด tunnel ไปยัง LIFF Frontend

สมมติว่า Docker Compose รันแล้ว และ `liff-web` อยู่ที่ `http://localhost:3000`

รันคำสั่ง:

```bash
ngrok http 3000
```

คุณจะได้ URL ประมาณ:

```text
https://random-id.ngrok.io
```

### 5.3 ตั้งค่าใน LINE Developers (LIFF)

1. ไปที่ LINE Developers Console  
2. เลือกโปรเจกต์ที่ใช้กับ OA ของคุณ  
3. สร้าง **LIFF App** ใหม่ (หรือแก้ของเดิม)
4. ตั้งค่า:
   - LIFF Endpoint URL = `https://random-id.ngrok.io`
5. บันทึกและจด **LIFF ID** ไว้ (นำไปใส่ `VITE_LIFF_ID` ภายหลังถ้าคุณจะผูก SDK)

### 5.4 ผูก Rich Menu กับ LIFF URL

ใน Rich Menu ให้ใช้ URL ดังเช่น:

- สมัครสมาชิก: `https://random-id.ngrok.io?action=register`
- เข้าสู่ระบบ: `https://random-id.ngrok.io?action=login`
- ดูคอร์ส: `https://random-id.ngrok.io?action=courses`
- วิธีใช้งาน: `https://random-id.ngrok.io?action=howto`
- ติดต่อเรา: `https://random-id.ngrok.io?action=contact`

ตัว `App.jsx` ใน `liff-web` จะอ่าน `action` จาก query string แล้วนำทางไปหน้าที่เกี่ยวข้องให้

จากนั้นทดสอบจากมือถือ:

1. เปิด Line → เข้า OA → กดปุ่มใน Rich Menu  
2. LIFF จะเปิดหน้าเว็บจาก `https://random-id.ngrok.io` ซึ่งยิงเข้ามาที่ `localhost:3000` บนเครื่องคุณ ผ่าน ngrok

---

## 6. ต่อเติมจากโปรเจกต์นี้

สิ่งที่โปรเจกต์นี้เตรียมให้:

- โครง Express API:
  - `/health` – เช็คว่า API + DB ทำงานหรือไม่
  - `/courses` (GET) – ดึงคอร์สจาก DB
  - `/courses` (POST) – สร้างคอร์ส (ใช้ใน admin)
  - `/admin/users` – ดึง user list
  - `/admin/auth/login` – Login สำหรับหลังบ้านด้วย email/password (response มี role + permissions)
  - `/admin/auth/refresh` – ขอ access token ใหม่จาก refresh token
  - `/admin/auth/logout` – เคลียร์สถานะฝั่ง client (stateless)
  - `/auth/line-login` – mock endpoint สำหรับ Login ด้วย Line ID
  - `/orders` – mock endpoint สำหรับสร้างออเดอร์
  - `/users/:userId/orders` – ดึงประวัติการสั่งซื้อของ user
  - `/payments/omise-webhook` – mock endpoint สำหรับรับ webhook จาก Omise

- โครง React:
  - LIFF:
    - Home, Courses, HowTo, Contact
  - Admin:
    - Dashboard, Courses (พร้อมฟอร์มสร้างคอร์ส), Users

คุณสามารถ:

- เพิ่มการเชื่อม **LINE LIFF SDK** ใน `liff-web/src/main.jsx` หรือ `App.jsx`  
- เพิ่ม **Omise integration** จริงใน `/orders` / `/payments`  
- เพิ่ม role, auth, และหน้าจออื่น ๆ ใน `admin-web`

---

## 7. การรันแบบไม่ใช้ Docker (ทางเลือก)

หากคุณอยากให้ dev ทดลองรันแต่ละส่วนด้วย Node/npm ปกติ:

### Backend API

```bash
cd src/api
npm install
npm run dev
# รันที่ http://localhost:4000
```

### LIFF Web

```bash
cd src/liff-web
npm install
npm run dev
# รันที่ http://localhost:3000
```

### Admin Web

```bash
cd src/admin-web
npm install
npm run dev
# รันที่ http://localhost:3001
```

แล้วเปิด Postgres แยกเอง หรือใช้ docker compose เฉพาะ db + api ก็ได้

---

## 8. สรุป Flow ที่สำคัญสำหรับคุณ

1. `docker-compose build && docker-compose up` → ระบบทั้งหมดขึ้นบน local  
2. `ngrok http 3000` → ได้ https URL สำหรับ LIFF  
3. เอา URL ไปตั้งใน LINE Developers เป็น LIFF Endpoint  
4. ผูก Rich Menu ให้ยิง URL พร้อม `action`  
5. ทดสอบจากมือถือได้เลย

คุณสามารถใช้โครงนี้เป็นจุดเริ่มต้น แล้วเสริม logic ต่าง ๆ ตามที่วาง requirement ไว้ เช่น กำหนด role ของ admin, จัดการคำสั่งซื้อ, เชื่อม Omise แบบเต็ม ฯลฯ

---

หากต้องการให้ช่วยต่อในส่วน:

- ออกแบบ Schema เพิ่ม (เช่น session คอร์ส, branch, instructor ละเอียดกว่านี้)  
- วางโครง Omise payment flow หรือ LIFF login flow แบบละเอียด  

บอกได้เลยครับ ผมจะช่วยอธิบายและออกแบบ flow เพิ่มจากโครงนี้ให้ต่อเนื่องได้ 😊
